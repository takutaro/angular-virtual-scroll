name: virtual_scroll
description: virtual-scroll component for AngularDart
version: 0.2.2
environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  angular: ^5.0.0

dev_dependencies:
  angular_test:
  angular_analyzer_plugin:
  build_runner:
  build_test:
  build_web_compilers:
  test:
